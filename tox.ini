# content of: tox.ini , put in same dir as setup.py
[tox]
envlist = py36

[testenv]
deps = 
	pytest
 	moto
 	boto3
 	s3fs
# https://github.com/travis-ci/travis-ci/issues/7940
commands = export BOTO_CONFIG=/dev/null && pytest